// Default implementation generated by toolkit.
// Can be modifid but might be re-genrated by toolkit, if allowed to
package {{.NAME}}

import (
	"context"
	"errors"

	{{ if .GOAPIPKG}}{{.GOAPIPKG}} "{{.GOAPIIMPORT}}" {{ end }}	
)

{{range $name, $v :=  .CMDS }} 
 func run{{$name}}(ctx context.Context,cmd *api.{{$name}}) (*api.{{$name}}Result, error){
		return nil, errors.New("command not implemented")
 }
{{- end }}

