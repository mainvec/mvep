// Handlers code generated by toolkit. Should not modify
{{- $NS := .NS }}
{{- $PKG := .GOPKG}}
package main

import (
	"context"
	"fmt"
	"os"

	{{ if .GOIMPORT}}{{.GOPKG}} "{{.GOIMPORT}}"{{ end }}
	"github.com/mainvec/mvep/runtime/go/mvep/cli"
	"github.com/mainvec/ugo/cli"
)

// version is injected at release build time via the linker:
//
//	go build -ldflags "-X main.version=vX.Y.Z"
//
// When empty, resolveVersion falls back to the module version (populated for
// `go install …@vX.Y.Z`) and finally to the {{.VERSIONCONST}} constant.
var version = ""

func resolveVersion() string {
	if version != "" {
		return version
	}
	if bi, ok := debug.ReadBuildInfo(); ok && bi.Main.Version != "" && bi.Main.Version != "(devel)" {
		return bi.Main.Version
	}
	return {{.VERSIONCONST}}
}

func main() {
	pkg := {{.GOPKG}}.NewPackage()
	runner := {{.GOPKG}}.NewCommandRunner()
	app := cli.New(pkg.Describe(), &cli.LocalExecutor{Runner: runner})
	app.Root().Version = resolveVersion()

	err := app.Run(context.Background())
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error: %v\n", err)
		os.Exit(cli.ExitCode(err))
	}
}