{{- $CLINAME := print .NAME "cli" }} 
{{- $APINAME := .APINAME}}
/*
{{ if .Project.Copyright }}{{ .Project.Copyright }}{{ end }}
{{ if .Legal.Header }}{{ .Legal.Header }}{{ end }}
*/
package cmd
import (
	"fmt"
	"log"

	{{$CLINAME}} "{{.BASE}}/{{$CLINAME}}"
	{{$APINAME}} "{{.BASE}}/{{$APINAME}}"
	"github.com/spf13/cobra"
)
// {{ .CmdName }}Cmd represents the {{ .CmdName }} command
var {{ .CmdName }}Cmd = &cobra.Command{
	Use:   "{{ .CmdName }}",
	{{- if .Cmd.CmdAlias }}
	Aliases: []string{"{{.Cmd.CmdAlias}}"},
	{{- end }}
	Short: "A brief description of your command",
	Long: `A longer description that spans multiple lines and likely contains examples
and usage of using your command. For example:
Cobra is a CLI library for Go that empowers applications.
This application is a tool to generate the needed files
to quickly create a Cobra application.`,
	Run: func(cmd *cobra.Command, args []string) {
		wocmd := &{{$APINAME}}.{{ .CmdName }}{}
		fmt.Println("{{ .CmdName }} called")
		result, err := {{$CLINAME}}.Client.Run{{ .CmdName }}(wocmd)
		if err != nil {
			log.Fatal(err)
		}
		fmt.Printf("Result is: %v", result)
		
	},
}
func init() {
	rootCmd.AddCommand({{ .CmdName }}Cmd)
	// Here you will define your flags and configuration settings.
	// Cobra supports Persistent Flags which will work for this command
	// and all subcommands, e.g.:
	// {{ .CmdName }}Cmd.PersistentFlags().String("foo", "", "A help for foo")
	// Cobra supports local flags which will only run when this command
	// is called directly, e.g.:
	// {{ .CmdName }}Cmd.Flags().BoolP("toggle", "t", false, "Help message for toggle")
}